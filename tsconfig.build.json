{
	"extends": "./tsconfig.json",
	// Sans "include", tsc remonte le rootDir à la racine du projet (prisma.config.ts,
	// prisma/seed.ts) et émet dans dist/src/ — alors que start:prod attend dist/main.js.
	"include": ["src/**/*"],
	"exclude": ["node_modules", "test", "dist", "**/*spec.ts"]
}
