info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/auth/register
  body:
    type: json
    data: |-
      {
        "email": "test@example.com",
        "password": "Oasis91350?",
        "name": "Jean Dupont"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
